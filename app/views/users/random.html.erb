<h1>Random Level</h1>
<a href="/random"><section id="newGame">Replay?</section></a>

<% ('a'..'z').to_a.shuffle.each do |letter| %>
  <div class="div<%= letter.upcase %>">
    <%= letter %>
  </div>
<% end %>
