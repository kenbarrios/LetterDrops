<h1>Alphabetized Level</h1>
<a href="/alpha"><section id="newGame">Replay?</section></a>

<% ('a'..'z').to_a.each do |letter| %>
  <div class="div<%= letter.upcase %>">
    <%= letter %>
  </div>
<% end %>
